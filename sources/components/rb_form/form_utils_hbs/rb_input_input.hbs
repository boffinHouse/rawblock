<input class="form-input" type="{{#if type}}{{ type }}{{ else }}text{{/if}}" id="dummy-{{ type }}-{{ id }}"

	{{#each attrs}}
		{{@key}}="{{this}}"
	{{/each}}

	{{#each booleans}}
		{{this}}="{{this}}"
	{{/each}}

		/>
