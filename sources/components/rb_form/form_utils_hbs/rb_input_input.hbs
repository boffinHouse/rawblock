<input type="{{#if type}}{{ type }}{{ else }}text{{/if}}" {{#is type "radio"}}name="{{name}}"{{/is}}
		id="{{name}}-{{ id }}"

	{{#each attrs}}
		{{@key}}="{{this}}"
	{{/each}}

	{{#each booleans}}
		{{this}}="{{this}}"
	{{/each}}

	{{#if invalid}}
		class="form-user-error"
	{{/if}}
		/>
